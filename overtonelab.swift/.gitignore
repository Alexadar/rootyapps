# Downloaded oracle reference data — never committed (see CommaKit-tools/oracles.manifest)
**/Fixtures/
build/
.build/
*.xcodeproj
.DS_Store

# Generated marketing media — screenshots, reels, audio (regenerated by the pipeline).
# Source config (params.yaml, metadata.md) is kept.
marketing/raw/
marketing/**/*.png
marketing/**/*.jpg
marketing/**/*.mp4
marketing/**/*.mov
marketing/**/*.wav
tools/recordwindow

# Belt-and-suspenders — video/audio/archives/objects are never source here.
# (App-icon and asset-catalog PNGs are deliberately NOT ignored — they are tracked source.)
*.mp4
*.mov
*.m4v
*.wav
*.mp3
*.aac
*.zip
*.o
*.a
